import 'package:flutter/material.dart';
class HostelRule extends StatelessWidget {
  const HostelRule({super.key});


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Hostel Rule"),
        backgroundColor: Colors.red,
      ),
      body: const Center(
        child: Text("Hello"),
      ),
    );
  }
}

